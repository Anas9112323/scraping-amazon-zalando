# Lancer le scrape tous les jours à 3h00 (heure locale de la machine)
#
# 1) Rendre le script exécutable (une fois) :
#    chmod +x "/Users/Anas/Documents/cours-scraping-pipeline/scripts/cron_scrape_3h.sh"
#
# 2) Ouvrir l’éditeur crontab :
#    crontab -e
#
# 3) Ajouter cette ligne (adapter le chemin si besoin) :
#
0 3 * * * /Users/Anas/Documents/cours-scraping-pipeline/scripts/cron_scrape_3h.sh
#
# Le script lance : python run.py --append
# (ajoute les lignes au CSV sans répéter l'en-tête).
# Pour un premier fichier propre : lancer une fois à la main
#   python run.py
# sans --append, puis laisser le cron avec --append.
#
# Sortie : logs/cron_3h.log (START/PROCESS DONE du run.py + marqueurs début/fin cron)
# Logs détaillés : logs/scrape_*.log
