# Planification — Linux / serveur (crontab)
#
# Tous les jours à 15h30 (heure locale) + Slack via session_2/.env :
#
# chmod +x ".../session_2/crontab.sh"
# crontab -e
#
# 30 15 * * * /bin/bash /Users/Anas/Documents/cours-scraping-pipeline/session_2/crontab.sh
#
# Sur macOS + Documents, préférer LAUNCHD.txt (launchd).
